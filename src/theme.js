import { createTheme } from "@material-ui/core";

const theme = createTheme({
    palette: {
        primary: {
            main: "#264653",
        },
        secondary: {
            main: "#2a9d8f",
        },
        accent: {
            main: "#e9c46a",
        },
        background: {
            main: "ffffff",
        },
    },
});

export default theme;
